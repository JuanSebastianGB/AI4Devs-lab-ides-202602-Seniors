# PostgreSQL connection. Prisma requires DATABASE_URL (dotenv does not expand ${VAR} in values).
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/DATABASE_NAME

# Server port. Must match the host:port used by frontend REACT_APP_API_URL.
PORT=3001

# Optional: directory for uploaded files (default: backend/uploads)
# UPLOAD_DIR=./uploads
